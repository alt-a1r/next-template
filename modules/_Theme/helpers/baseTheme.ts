import { COLORS } from '@/theme';

const baseTheme = {
  token: {
    radiusBase: 8,
    borderRadius: 8,
    colorTextBase: COLORS.DARKEST_GRAY,
    colorPrimary: COLORS.BLUE_PRIMARY,
    colorLink: COLORS.BLUE_PRIMARY,
    colorLinkHover: COLORS.BLUE_PRIMARY_HOVER,
    colorLinkActive: COLORS.BLUE_PRIMARY_ACTIVE,
  },
};

export default baseTheme;
